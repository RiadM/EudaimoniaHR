<%= form_for @job do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>
      <ul>
      <% @job.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, id: :job_name %>
  </div>

<div class="field">
    <%= f.label :skill_list, "Skills (separated by commas)" %><br />
    <%= f.text_field :skill_list %>
  </div>

  <div class="field">
    <%= f.label :weight %>
    <%= f.number_field :weight, id: :job_weight %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
